server {
    listen 443 ssl;

    server_name localhost;

 	ssl_certificate /etc/nginx/ssl/nginx.crt;  # Path to your SSL certificate
    ssl_certificate_key /etc/nginx/ssl/nginx.key;  # Path to your SSL private key

    root /var/www/html;
    index index.php index.html index.htm;

    location / {
        try_files $uri $uri/ /index.php?$args;
    }

    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass wp-php:9000;  # Ensure this matches the WordPress service name and port
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.ht {
        deny all;
    }
}
